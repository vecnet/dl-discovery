<%= render 'did_you_mean' %>

<%= render 'constraints' %>

<%= render 'search_sidebar' %> <%# here for Dan to parse and unwind CSS %>

<%= render 'sort_and_per_page' %> <%# TODO will need to be replaced with non paginated js version %>